// Variáveis (let)
/* armazenar informações para acresentar no código*/

console.log('Variáveis');

let firstname = 'Felipe';
let lastname = 'Arruda'

console.log(firstname);
console.log(lastname);

// Constantes (const)
/* O const cria uma variável cujo o valor é fixo, ou seja, 
uma constante somente leitura. Isso não significa que o valor é imutável, 
apenas que a variável constante não pode ser alterada ou retribuída.*/

const middlename = 'Paula';

console.log(middlename);


